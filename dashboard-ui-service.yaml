apiVersion: v1
kind: Service
metadata:
  name: dashboard-ui-service
  namespace: app
spec:
  selector:
    dashboard-ui: first
  ports:
  - name: dashboard-ui-port
    protocol: TCP
    port: 3091
    targetPort: 3091